-if user_signed_in?
  %span Logged in as #{current_user.name}
  = link_to 'Salir', signout_path, class: 'pull-right btn'
-else
  = link_to (image_tag 'twitter-connect.png'), signin_path, class: 'pull-right'
